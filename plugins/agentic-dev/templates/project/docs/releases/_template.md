# <Product Name> <0.2.0> — <one line: the headline a user would care about>

<!-- Written at the version cut, from the USER's perspective — this is outreach raw
     material (website, social posts), not a changelog. Short and punchy: what can I use
     now? One screenshot per feature, no videos (videos are for meta-releases, later).
     No history, no process, no file names. -->

**Contains:** <F003, B004–B006, T002 — the package ids say what kind of release this is>

## <F003 · What you can do now>

<Two or three sentences, addressed to the user: what works now that did not before, and
why they would want it.>

![<what the screenshot shows>](../assets/releases/<version>-<slug>.png)

## Fixed

<One line per B package, in user language: "<what stopped being broken>". No stack traces,
no causes — the learning files hold those.>

## Under the hood

<One line total for the T packages, only if a user would notice anything at all —
"faster", "more reliable offline". Otherwise omit the section.>
